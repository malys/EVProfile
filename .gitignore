# Gradle
.gradle/
build/
**/build/

# Local config (never commit — contains local SDK paths)
local.properties

# mokup
mockup/

# Internal analysis (uncommitted) — generated working docs
analysis/DIFF_2.7.0.md
analysis/DIFF_2.7.0.full.diff
analysis/SOH_feasibility.md
analysis/FEATURE_BACKLOG.md

# Claude Code (config locale IDE)
.claude/

# Kotlin build cache
.kotlin/

# Android Studio
.idea/
*.iml
*.iws
*.ipr
.DS_Store

# Keystore & signing secrets (never commit)
*.jks
*.keystore
keystore.properties
signing.properties
*KEYSTORE_BASE64*

# Fichiers de log
*.log


# mise: mise.toml is COMMITTED (it describes the build). Only overloads
# local, machine-specific, are ignored.
mise.local.toml
.mise.local.toml
