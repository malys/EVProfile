# Gradle
.gradle/
build/
**/build/

# Local config (ne jamais committer — contient les chemins locaux SDK)
local.properties

# mokup
mockup/

# Analyse interne (non commitée) — docs de travail générés
analysis/DIFF_2.7.0.md
analysis/DIFF_2.7.0.full.diff
analysis/SOH_feasibility.md
analysis/FEATURE_BACKLOG.md

# Claude Code (config locale IDE)
.claude/

# Kotlin build cache
.kotlin/

# Android Studio
.idea/
*.iml
*.iws
*.ipr
.DS_Store

# Keystore & secrets de signature (ne jamais committer)
*.jks
*.keystore
keystore.properties
signing.properties
*KEYSTORE_BASE64*

# Fichiers de log
*.log


# mise : mise.toml est COMMITÉ (il décrit le build). Seules les surcharges
# locales, machine-spécifiques, sont ignorées.
mise.local.toml
.mise.local.toml
