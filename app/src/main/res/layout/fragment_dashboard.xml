<?xml version="1.0" encoding="utf-8"?>
<!--
    Le pager occupe tout le fragment : ses deux destinations sont des boutons de la barre
    du haut (dashboard_tab_controls / dashboard_tab_elk), pas une rangée d'onglets qui
    reprendrait 104 dp de hauteur sur un panneau de 720. La navigation reste explicite et
    tactile — le geste de balayage n'est jamais le seul chemin.
-->
<androidx.viewpager2.widget.ViewPager2
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dashboard_pager"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mg4_background"
    android:paddingStart="@dimen/spacing_lg"
    android:paddingEnd="@dimen/spacing_lg"
    android:paddingTop="@dimen/spacing_md" />
